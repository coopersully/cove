{
  "name": "@hearth/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "check": "tsc --noEmit",
    "dev": "set -a && . ../../.env && set +a && tsx watch src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@hearth/auth": "workspace:*",
    "@hearth/db": "workspace:*",
    "@hearth/shared": "workspace:*",
    "@hono/node-server": "^1.14.0",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.11.9",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@hearth/config": "workspace:*",
    "@types/node": "^25.2.3",
    "tsx": "^4.19.0"
  }
}
