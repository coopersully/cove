{
  "name": "@hearth/ws",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "check": "tsc --noEmit",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@hearth/gateway": "workspace:*",
    "@hearth/shared": "workspace:*"
  },
  "devDependencies": {
    "@hearth/config": "workspace:*",
    "@types/node": "^25.2.3",
    "tsx": "^4.19.0"
  }
}
