{
  "name": "@cove/ws",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "check": "tsc --noEmit",
    "dev": "set -a && . ../../.env && set +a && tsx watch src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@cove/auth": "workspace:*",
    "@cove/db": "workspace:*",
    "@cove/gateway": "workspace:*",
    "@cove/shared": "workspace:*",
    "drizzle-orm": "^0.45.1",
    "ioredis": "^5.6.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@cove/config": "workspace:*",
    "@types/node": "^25.2.3",
    "@types/ws": "^8.18.1",
    "tsx": "^4.19.0"
  }
}
